# Copy this file to .env and fill in your values

# MySQL — use the credentials Railway gives you for your MySQL service
DB_HOST=your-railway-mysql-host
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-db-password
DB_NAME=alphasigma

# JWT — generate a long random string, e.g.: openssl rand -hex 32
JWT_SECRET=replace-with-a-long-random-secret

# Your Vercel frontend URL (no trailing slash), e.g. https://alphasigma.vercel.app
FRONTEND_URL=https://your-app.vercel.app

# Railway sets PORT automatically — do not set this manually in Railway
# PORT=3001
